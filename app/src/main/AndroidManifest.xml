<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.capstone_project">

    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.Welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.MainActivity"
            android:label="@string/title_activity_main" />
        <activity android:name=".Authenticator.signUp" />
        <activity android:name=".Data.resetPassword" />
        <!-- <activity -->
        <!-- android:name=".logIn" -->
        <!-- /> -->
        <activity android:name=".Authenticator.Login" />
        <activity android:name=".Data.myProfile" />
        <activity android:name=".Data.viewUser" />
        <activity android:name=".Activities.ActivityOne" />
        <activity android:name=".ActivityTwo" />
        <activity android:name=".Activities.ActivityThree" />
        <activity android:name=".AlarmNotification" />
        <activity android:name=".Model.livePage" />
        <activity android:name=".Activities.plant1" />
        <activity android:name=".Activities.ActivityOne$About" />
        <activity
            android:name=".EditAlarm"
            android:label="Edit alarm" />

        <receiver
            android:name=".AlarmReceiver"
            android:process=":remote" />

        <activity android:name=".Data.history" />
        <activity android:name=".Data.graph" />
        <activity android:name=".Activities.About"></activity>
    </application>

</manifest>